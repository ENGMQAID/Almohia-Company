# النسخة المتصلة بالخادم — دليل التشغيل الحالي

## على هذا الجهاز
افتح START-SERVER.cmd واترك نافذته مفتوحة ثم افتح http://127.0.0.1:8787 وسجل الدخول. يلزم Node.js 24.14 أو أحدث، والملف يستخدم نسخة Codex إن لم توجد node في PATH. البديل من مجلد المشروع:

```text
node --env-file-if-exists=server/.env server/main.mjs
```

قاعدة البيانات تحفظ في server/data/group.sqlite. إغلاق الخادم يوقف الموقع والجدولة محليًا. فتح index.html مباشرة يبقى معاينة المتصفح القديمة ولا يشغّل الربط بالخادم.

## البيانات والحسابات
- يبدأ الخادم بسجلات فارغة للشركتين؛ بيانات التجربة في المتصفح لا تنقل تلقائيًا.
- أضف الموظفين أو استورد Excel، ثم اربط الهوية من «حسابات الموظفين». رمز التفعيل يستخدم مرة واحدة خلال 24 ساعة لاختيار كلمة المرور.
- حساب المدير الحالي يهيأ من auth-bootstrap.local.js على هذا الجهاز. الخادم لا يرسل محتواه للمتصفح. الملف خارج Git.
- التحقق من كلمات المرور في الخادم والجلسة HttpOnly وSameSite=Strict مع CSRF، وSecure عند ضبط HTTPS للإنتاج.
- الموظف لا يقرأ وثائق الإدارة أو بيانات الشركة الثانية، ولا يعتمد طلب إجازته بنفسه.
- يحفظ التغيير بعد استجابة الخادم. إذا تغيرت البيانات من جلسة أخرى يرفض الخادم الكتابة القديمة ويحمّل النسخة الأحدث.
- طلبات الإجازات والمهام تربط برقم الموظف داخليًا. الأسماء المتكررة دون تحديد الموظف ترفض عند الربط.
- هذه المرحلة تدعم دوري المدير والموظف. أدوار المدير المباشر والمدير الأعلى والمالك واعتمادهم وفق الهيكل ما زالت تحتاج تنفيذًا. تجاوز اليومين ينشئ إشعار تصعيد للإدارة، ولا يمنح دورًا غير موجود.

## تلجرام والجدولة
المفتاح ومعرف محادثتك محفوظان في server/.env على جهازك خارج Git والحزم. TELEGRAM_ENABLED=true يفعّل إرسال الطابور أثناء تشغيل الخادم. أعد تشغيل الخادم إذا غيّرت ملف الإعدادات.

تنشأ الإشعارات عند إضافة موظف أو طلب إجازة أو مهمة. دورة الفحص كل دقيقة تنشئ تذكيرات الوثائق ووثائق السيارات عند 60 و30 و7 أيام ويوم الانتهاء، وتصعيد الإجازات بعد 48 ساعة. إذا فاتت أيام أثناء التوقف، ترسل المرحلة المناسبة للوضع الحالي دون إغراق برسائل كل المراحل السابقة.

الطابور في SQLite ويسجل نجاح الإرسال ويعيد محاولة الفشل. يلغي تنبيه التجديد المعلّق إذا تغير تاريخ الوثيقة، ويلغي تصعيد الطلب المعلّق إذا حسم الطلب. قد تتكرر رسالة في حالة نادرة إذا نجح الإرسال وانقطع الاتصال قبل تسجيله؛ sendMessage لا يوفر مفتاح منع تكرار.

الرسالة مختصرة: الحدث والشركة ورابط البوابة إذا ضبط PORTAL_URL، دون بيانات الموظف أو تفاصيل الوثيقة. إذا لم يجهز الرابط ترسل دون رابط. راقب عدد المرسل والمعلق من «ربط تلجرام» بحساب المدير.

## GitHub والاستضافة
- حزمة group-github-source.zip للكود، بلا إعداداتك السرية أو قاعدة البيانات. يفضل مستودع خاص.
- حزمة group-local-preview.zip تشمل مهيئ المدير الخاص، لكنها لا تضم مفتاح تلجرام ولا قاعدة البيانات. لا ترفعها إلى GitHub.
- بيانات الموظفين والمرفقات تحفظ في قاعدة البيانات الخاصة؛ GitHub للكود.
- النسخة تحتاج استضافة Node.js 24 مع قرص دائم خاص لـ SQLite. Dockerfile مرفق للخدمات الداعمة للحاويات.
- إعدادات الإنتاج: NODE_ENV=production، HOST=0.0.0.0، PUBLIC_ORIGIN=https://نطاقك، PORTAL_URL=https://نطاقك/portal.html، DATABASE_PATH على القرص الدائم، ADMIN_BOOTSTRAP_PATH ملف المدير من مخزن أسرار خاص لأول تهيئة.
- بعد إنشاء المدير في القاعدة لا يحتاج التشغيل التالي ملف التهيئة. أضف أسرار تلجرام من لوحة الاستضافة، لا من المستودع.
- يلزم HTTPS ونسخ احتياطي مجدول مع اختبار الاستعادة. شغّل نسخة خادم واحدة حاليًا؛ التوسع لعدة نسخ يحتاج مراجعة تخزين وقفل المهام وحدود الدخول.
- GitHub Pages وحده لا يشغّل الخادم والقاعدة والجدولة. لم تنشر استضافة عامة بعد.

## الاختبارات والحدود
شغّل npm test لفحص API ببيانات اصطناعية وتلجرام وهمي. تختبر الدخول والجلسات والتفعيل وفصل الشركات ومنع كشف الأسرار وتضارب الكتابة والطابور. لا ترسل الاختبارات رسائل حقيقية.

المرفق حتى 1 ميجابايت PDF/PNG/JPG، والتحديث الواحد حتى 32 ميجابايت. المرفقات داخل SQLite الخاصة حاليًا. نقلها إلى مخزن ملفات، النسخ الاحتياطي، صلاحيات المشرفين والمالك، وفحص الواجهة بصريًا على الاستضافة خطوات قبل الإطلاق المؤسسي.

مرجع SQLite المستخدم: https://nodejs.org/docs/latest-v24.x/api/sqlite.html
