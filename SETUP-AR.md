# دليل النسخة وخطوة النشر التالية

هذا دليل المعاينة المحلية السابقة. للنسخة المتصلة بقاعدة البيانات والخادم والجدولة راجع SERVER-SETUP-AR.md.

## الملفات والحزمتان
- `group-local-preview.zip`: نسخة خاصة لجهازك تحتوي مهيئ حساب المدير المحلي. لا ترفع هذه الحزمة إلى GitHub أو استضافة عامة.
- `group-github-source.zip`: الكود والملفات العامة ودليل الربط، دون مهيئ حسابك ودون كلمات مرور أو بيانات موظفين حقيقية. حساب المدير لا يعمل تلقائيًا في هذه الحزمة حتى نربط المصادقة بالخادم.
- فك الضغط أولًا ثم افتح index.html. مجلد assets وملفات CSS وJavaScript يجب أن تبقى مع الصفحة.
- سجلات تجربتك السابقة محفوظة في متصفحك، وليست داخل ZIP. فتح النسخة من مسار مختلف قد ينشئ مساحة تخزين محلية مختلفة.
- الاسم المعروض لفورايفر أصبح «مؤسسة فايز المحيا للتجارة» بحسب طلبك الأخير. مفتاح الشركة الداخلي بقي كما هو لحفظ السجلات السابقة.

## ما يعمل الآن
الصفحة العامة، خلفية مصورة متحركة يمكن إيقافها، بطاقات العلامات كاملة دون قص، شعار وألوان لكل شركة، إضافة موظف أو استيراد مجموعة Excel، تقارير الشركة بتصفية وتصدير CSV وطباعة PDF، مركز إشعارات محلي بعد الإضافة والتعديل والتنبيهات القريبة. الموظف لا يرى إشعارات الوثائق الخاصة بالإدارة. صورة الخلفية والصور الخارجية وخطوط Google تحتاج اتصالًا بالإنترنت؛ الشعارات المحلية مرفقة.

## تلجرام — إعداد مرة واحدة
1. افتح https://t.me/BotFather وأرسل /newbot، ثم اختر اسم البوت واسم المستخدم. احتفظ بالمفتاح بسرية.
2. افتح البوت الجديد واضغط Start من حسابك الذي تريد وصول الإشعارات إليه.
3. على الخادم لاحقًا، انسخ server/.env.example إلى server/.env وضع المفتاح هناك. لا تضعه في صفحة HTML ولا ترسله في رابط ولا ترفعه إلى GitHub.
4. باستخدام Node.js 22 أو أحدث، من مجلد المشروع شغّل:

```text
node --env-file=server/.env server/chat-id.mjs
```

5. ضع الرقم الناتج في TELEGRAM_CHAT_ID، وحدد رابط البوابة المنشور HTTPS في PORTAL_URL.
6. لإرسال اختبار يدوي إلى محادثتك:

```text
node --env-file=server/.env server/test-telegram.mjs
```

لم ينفذ المساعد هذه الخطوات ولم يرسل أي رسالة. المرجع الرسمي: https://core.telegram.org/bots/tutorial وhttps://core.telegram.org/bots/api#sendmessage

## الإرسال التلقائي — يحتاج الخطوة القادمة
- ملف server/telegram.mjs جاهز للإرسال من الخادم ويخفي بيانات الموظف وتفاصيل الوثيقة من نص الرسالة.
- server/expiry-events.mjs يحسب تذكيرات 60 و30 و7 أيام ويوم الانتهاء، بمعرّف يميز الوثيقة وتاريخ التجديد والمرحلة.
- server/dispatch.mjs نموذج طابور خادم، يحفظ نجاح الإرسال ويعيد محاولة الفشل دون إظهار المفتاح. لا تشغله بالتوازي؛ قبل الإنتاج نستبدل JSON بطابور في قاعدة البيانات مع قفل للمهام. لا توجد ضمانة عدم التكرار عند انقطاع الاتصال بعد إرسال الرسالة وقبل تسجيل النجاح.
- بعد ربط قاعدة البيانات نجعل أحداث الطلبات والتصعيد والتجديد تكتب في الطابور، ونشغّل مهمة دورية في الاستضافة. الجدولة تتضمن استدراك المواعيد الفائتة إذا تعطلت المهمة. الجدولة والتزامن والاستدراك لم تنفذ هنا.
- لا توجد نقطة إرسال عامة من المتصفح كي لا ينكشف مفتاح البوت أو يستطيع الزائر إرسال رسائل باسم النظام.
- لا يمكن للخادم قراءة localStorage من أجهزة الموظفين؛ لا يوجد إرسال تلقائي من هذه النسخة المحلية.

## قبل رفع البيانات الحقيقية
الخطوة التالية ربط حسابات وصلاحيات على الخادم، وقاعدة بيانات تفصل الشركتين، وتخزين خاص للمرفقات. بعدها نرفع الكود إلى مستودع خاص في GitHub وننشر الموقع، ثم ننقل بيانات الموظفين إلى قاعدة البيانات عبر الاستيراد. GitHub لحفظ الكود؛ بيانات الموظفين والوثائق لا ترفع كملفات عامة في المستودع.

GitHub Pages يستضيف واجهة ثابتة ولا يشغّل وحده خدمة Node أو المهام الدورية لهذا المشروع. اختيار الخادم وربط الحسابات والنشر لم ينفذ بعد.
